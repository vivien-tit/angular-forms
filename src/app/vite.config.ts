export default defineConfig({
  server: {
    open: '/templates' // Ouvre directement la bonne page
  }
});

function defineConfig(arg0: {
    server: {
        open: string; // Ouvre directement la bonne page
    };
}) {
    throw new Error("Function not implemented.");
}
